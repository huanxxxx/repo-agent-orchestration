interface:
  display_name: "Repository Agent Orchestration"
  short_description: "Coordinate useful contexts, ownership, and delivery"
  default_prompt: "Use $repo-agent-orchestration to finish this repository task with appropriate context splitting, clear ownership, proportional verification, and recoverable handoff."
