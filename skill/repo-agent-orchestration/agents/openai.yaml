interface:
  display_name: "Repository Agent Orchestration"
  short_description: "Route peer tasks and same-task subagents safely"
  default_prompt: "Use $repo-agent-orchestration to distinguish peer tasks from same-task subagents, then coordinate safe dispatch, verification, and closure."
