interface:
  display_name: "Repository Agent Orchestration"
  short_description: "Route, dispatch, review, and integrate repository agents"
  default_prompt: "Use $repo-agent-orchestration to coordinate this repository task with safe parallel dispatch, verification, and closure."
